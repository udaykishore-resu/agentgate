# The local control-plane token-signing key is generated by `make dev-keys`
# (invoked automatically by `make run-stack`) and must never be committed.
# Every deployed environment projects this key from Key Vault / Secrets Manager
# via the CSI driver instead — see deploy/k8s/base/controlplane.yaml.
*.pem
*.key
!.gitignore
